version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build steps needed for static HTML/CSS/JS"
    build:
      commands:
        - echo "Skipping build"
  artifacts:
    baseDirectory: frontend
    files:
      - '**/*'
  cache:
    paths: []